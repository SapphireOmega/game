#version 330 core

in vec2 position;
in vec3 vcolor;
in vec2 vtexcoord;

out vec3 fcolor;
out vec2 ftexcoord;

uniform mat4 model;
uniform mat4 view;

void main()
{
	fcolor = vcolor;
	ftexcoord = vtexcoord;
	gl_Position = view * model * vec4(position, 0.0, 1.0);
}

